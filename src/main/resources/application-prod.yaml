server:
  port: ${PORT}

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marceliino/ead-config-server-repo
          default-label: main

encrypt:
  key: PBN79CFDPA5diaEOVyz4D22sAs8KZLHlkrBDIvVLXpgnN5bH7UqmlmVfrpAGMz22

ead:
  configServer:
    username: ${CONFIG_SERVER_USERNAME}
    password: ${CONFIG_SERVER_PASSWORD}

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL}
  instance:
    hostname: ${APP_DOMAIN_NAME}
    non-secure-port: 80